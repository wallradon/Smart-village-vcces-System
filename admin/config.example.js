/**
 * Example Configuration Module
 * คัดลอกไฟล์นี้ไปตั้งชื่อเป็น config.js แล้วตั้งค่า API_BASE_URL ตามที่ต้องการ
 */
var CONFIG = {
    // URL สำหรับเชื่อมต่อ API
    API_BASE_URL: 'http://localhost:3000/api'
};

Object.freeze(CONFIG);
